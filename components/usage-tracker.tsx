"use client";

// Usage tracker UI removed. Keep a minimal stub component so existing imports
// don't break while the rest of the codebase is cleaned up.

export default function UsageTracker() {
  return null;
}
